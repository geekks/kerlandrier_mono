services:
  kerlandrierfront:
    image: kerlandrierfront
    build:
      context: .
      dockerfile: ./Dockerfile
    env_file: ".env"
    ports:
      - 8090:5173
    develop:
      watch:
        - action: sync
          path: .
          target: /app
          ignore:
            - node_modules